class Solution{
public:	
	int sum=0;
	int divisibleBy11 (string S)
	{
	    for(int i=0;i<S.size();i++){
	        int digit=S[i]-'0';
	        sum=sum*10+digit;
	        sum=sum%11;
	    }
	 return sum==0;
	}
